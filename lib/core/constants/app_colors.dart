import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFF050A14);
  static const surface = Color(0xFF0B132B);

  // Primary
  static const primary = Color(0xFF5B5FEF); // roxo botão
  static const primaryDark = Color(0xFF3E42C9);

  // Accent
  static const accent = Color(0xFFD08C3A); // dourado
  static const accentDark = Color(0xFF9E6726);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF9CA3AF);

  // Inputs
  static const inputBackground = Color(0xFF111827);
  static const inputBorder = Color(0xFF1F2937);
}
